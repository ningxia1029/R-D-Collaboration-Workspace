# PLM 研发协同平台——交付进度报告

**更新日期**：2026-08-20
**执行范围**：工作台阶段 0–5 生产化加固
**当前结论**：代码级加固和自动构建已完成一轮，但尚未达到生产验收；保守状态为“进行中”。

## 一、当前进展

| 领域 | 当前状态 | 本轮证据 |
|---|---|---|
| 核心工作台 | 已具备 MVP 闭环 | 项目、任务、BOM、参数、ECR/ECO、PLM、知识库、资源与审计页面/API 均存在 |
| 安全与权限 | 已完成一轮 P0 修复 | 实体项目归属、批量 ID、文档/评论/工时/依赖/追溯/搜索权限已收口；密码哈希不再从管理员 API 返回 |
| 桌面更新 | 已改为安全下载策略 | 固定英文资产名、GitHub Release、重定向限制、SHA-256 校验、手动切换新包；产物不内嵌数据库密钥 |
| 数据可靠性 | 迁移与运维材料已生成 | PostgreSQL baseline、索引迁移、备份/恢复脚本、事务化 ECR/ECO 状态与审批审计 |
| 功能补齐 | 首批完成 | 工时 UI、依赖查看/删除、追溯实体选择、ECO 影响面输入与审批历史、BOM 联动修正 |
| 自动化 | 已建立基础门禁 | 单元测试 6/6、TypeScript、Prisma、生产构建通过；Windows CI 与 Dependabot 已配置 |

## 二、本轮验证结果

| 命令 | 结果 | 能证明 | 不能证明 |
|---|---|---|---|
| `npm run test` | 通过，6/6 | 更新版本/校验清单、文档权限范围、状态 CAS 契约 | API、UI 和真实数据库并发正确性 |
| `npm run typecheck -- --incremental false` | 通过 | 当前 TS 类型闭合 | 运行时业务正确性 |
| `npx prisma validate` | 通过 | Schema 语法与关系有效 | 现有生产库可直接迁移 |
| `npm run build` | 通过 | Next.js 生产构建可完成 | 真机 GUI、桌面包和生产部署 |
| `npm run build:desktop` | 通过 | EXE/ZIP 可生成，秘密扫描、哈希与 provenance 门禁生效 | 正式 HTTPS 登录、签名与升级回退 |
| EXE/绿色版本机冒烟 | 通过 | 两种交付形态均能打开并响应首次配置窗口 | 干净 Windows、托盘、业务操作与在线更新全链路 |
| `npm audit --omit=dev` | 未通过 | 发现 2 严重、4 高危依赖风险 | 不能据此判定所有漏洞均可利用 |

## 三、重要纠偏

旧报告中的“阶段 3 90%”“P7/P8/P9 实际已完成”“云端全链路已验收”等结论缺少本轮可复现的完整证据，不能继续作为生产验收结论。本报告改为按代码检查、自动验证、隔离数据库演练、真机人工验收四个层级分别记录。

旧桌面更新方案存在导出不匹配、托盘事件未注册、代理下载、缺少完整性校验和尝试覆盖临时解压 exe 等问题，本轮已重构。旧资产不能自动继承新安全结论，必须重新构建和真机验收。

## 四、尚未完成与发布阻断

1. `npm audit` 的 Auth.js、Next.js/PostCSS、SheetJS 风险未清零。
2. baseline 与索引迁移尚未在隔离 PostgreSQL 上演练，备份恢复也未执行。
3. 四角色 × 项目成员边界、关键 API 和浏览器 GUI 尚未完成系统验收。
4. 桌面包未用正式 HTTPS 服务地址重新构建；更新全流程未在干净 Windows 真机验证。
5. 当前 EXE 未签名，provenance 记录 `sourceDirty=true`；正式包必须在提交后从干净 HEAD 重建。
6. 未创建提交、未推送、未发布 Release；这些动作需要明确授权。

## 五、交付物

- `docs/PRODUCTION_HARDENING_PLAN.md`：阶段 0–5 方案、结果、风险和回滚。
- `docs/ACCEPTANCE_CHECKLIST.md`：自动化、数据库、权限、安全、桌面发布验收清单。
- `docs/DATABASE_OPERATIONS.md`：迁移、备份与恢复操作边界。
- `prisma/migrations/`：基线迁移与工作台索引迁移。
- `.github/workflows/ci.yml`、`.github/dependabot.yml`：持续验证和依赖更新。
- `dist/PLM-Workspace-v1.0.1-portable.exe`：本机验收包，SHA-256 `b54a287444321183ba6807c08f4f4e966a3ba3526c16c7b7249e381232a83585`。
- `dist/PLM-Workspace-v1.0.1-green.zip`：绿色版，SHA-256 `c27e65e52a60384756949e8b485c9be41d789ba40b25e1ac963a9027cda7469a`。
- `dist/SHA256SUMS.txt`、`dist/BUILD-PROVENANCE.json`：完整性与源码来源证据。

## 六、下一步顺序

1. 处置依赖漏洞并扩充 API/RBAC/E2E 测试。
2. 在隔离数据库完成迁移、备份、恢复和抽样。
3. 完成浏览器角色矩阵与桌面真机验收。
4. Gate 2 代码评审通过后，再决定提交、推送和正式发版。

## 七、2026-08-13 工作台体验与权限验收增量

### 已确认问题

- 用户现场使用的 `PLM平台-1.0.1-便携版.exe` 为 2026-08-07 旧包：本机启动 HTTP 服务，但直接访问新加坡 Neon PostgreSQL。日志存在 P1001、连接关闭；系统 Node 24 另复现 Windows TLS 凭据错误，因此慢加载和偶发空列表不能归因于纯前端。
- 旧中文包与 `PLM-Workspace-v1.0.1-portable.exe` 都是便携自解压包，不注册 Windows 安装/卸载项，所以不会提示卸载。两个旧包使用不同临时目录，磁盘可并存；应用单实例锁用于避免同时运行，但旧包仍不应继续使用。
- 李工是 TH-100 和 GW-20 两个项目的显式成员；项目负责人均为张工。“成员可见”与“负责人”是不同字段，因此李工看到张工负责的项目符合当前权限模型。

### 本轮代码优化

- 项目设置新增名称、状态、关联产品、日期和描述编辑；生命周期继续走受控阶段流转；进度说明为“已完成任务数 / 任务总数”自动计算。
- 项目列表移除逐项目任务统计的 N+1 往返；工程变更页复用项目上下文，少一次重复请求。
- 项目加载失败展示明确错误和重试，不再把连接失败伪装为“暂无数据”；前端请求超过 20 秒给出网络/服务器提示。
- 任务、BOM 和评论控件按项目有效角色收口：viewer 只读，engineer 仅可更新本人负责/创建的任务，不展示无权批量操作；负责人选项仅来自当前项目成员。
- 便携包增加欢迎页：解压开始前可以取消，并明确说明解压期间为防止文件损坏会暂时禁用关闭；便携包仍不是 Windows 安装程序，不产生卸载项。

### 验收证据与边界

- `npm test`：74/74 通过，含四角色权限策略、viewer 评论、文档范围与状态 CAS 回归。
- `npm run typecheck`：通过。
- `npm run build`：通过，45/45 页面生成完成。
- `npm run build:desktop`：通过，NSIS 报告 2 pages；EXE、ZIP、SHA256SUMS、provenance 已生成。
- 为避免不同内容继续共用 1.0.1 版本号，本轮验收包升级为 1.0.2。
- 新 EXE SHA-256：`0a07644571cb6c41151579e9ac477596018b0f5f7f9118ce7ed0bd5cec1de671`。
- 新 ZIP SHA-256：`e329dc9d9cb3cf6d0e6ae770d5e2adc43e893a3fa785066bd0eb95daffe33e9d`。
- 旧包云端数据只读实测：admin、pm、engineer 可见两个项目，viewer 仅见 TH-100；四类账号对 TH-100 的项目、任务、BOM、ECO、ECR、知识库和评论 GET 均为 HTTP 200。单次请求约 0.9–6.4 秒，印证云数据库往返偏慢；未对共享云端演示数据执行写操作 UAT。
- 新包 `serverUrl` 为空、`sourceDirty=true`、`NotSigned`，只可用于本机验包；正式 HTTPS 服务、干净 Windows 四角色写操作、签名与升级/回滚仍是生产阻断。

## 八、2026-08-20 云端就绪与账号生命周期增量

### 已完成

- 新增生产运行配置门禁和 `live/ready` 探针；生产只接受网关限流，公开占位 Secret、演示模式和错误数据库配置会 fail closed。
- 新增 Node 24 / PostgreSQL 16 多阶段 Docker 构建、隔离 UAT 编排、迁移与受控 seed；最新源码 runner 重建成功，Next.js 48/48 页面生成，standalone 环境文件数为 0。
- 账号生命周期补齐管理员建号、强密码、首次强制改密、密码变更时间、会话版本撤销和并发 CAS；继续有意关闭公共自助注册。
- 四角色 PostgreSQL 16 真实 UAT 通过 1/1：覆盖 admin、pm、engineer、viewer 登录，PM/工程师业务写入，工程师本人任务边界，viewer 只读与非成员拒绝。
- 仓库门禁：`npm test` 123/123、`agent:eval` 68/68、TypeScript、Prisma、生产构建和 `npm audit --audit-level=high`（0 漏洞）均通过。

### 当前结论与剩余阻断

系统已达到“可部署的企业内网/云端 UAT 版本”，但仍不是正式公网生产完成态。尚需确定云平台与区域、配置域名/DNS/TLS、生产网关限流和 Secret 轮换，决定企业 IdP/账号开通流程，并完成远端 CI、备份恢复、签名桌面包及升级回退验收。当前工作树仍有未提交改动，未执行 Git 提交、推送或 Release。
