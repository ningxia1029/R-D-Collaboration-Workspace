# 2026-08-05 工作日志

## PLM 研发协同平台（公司级）搭建

基于《个人项目管理应用开发文档.docx》扩展为公司级 PLM + 项目管理 + 工程知识库一体化平台。

**技术决策（用户确认）**：Next.js 14 全栈 + Prisma/SQLite、React + AntD 5、完整可运行交付。
**计划文件**：`C:\Users\Administrator\.workbuddy\plans\cosmic-thunder-curie.md`

### 已完成
- 完整 Prisma schema（24 表；原文档 6 表通过 @@map/@map 保留表名与 snake_case 字段）
- RBAC：NextAuth v5（auth.config.ts 拆分给 Edge middleware，避免引入 Prisma）+ lib/rbac.ts（requirePerm + ProjectMember 项目级数据权限）
- 服务层：taskService（依赖检环）、bomService（齐套率、Delayed+isCritical 自动 Block/恢复组装任务）、changeService（ECR→ECO 两级状态机 + SequenceCounter 发号）、projectService（生命周期机 CONCEPT→RD→PILOT→MP→EOL 守卫）、kbService（版本历史）、searchService（FTS5 trigram + LIKE 降级）、dashboardService（健康度/Deadline/负载）
- 全部 API 路由（projects/tasks/bom/specs/ecrs/ecos/plm/documents/search/dashboard/admin…）
- 前端：AppShell+Ctrl+K 全局搜索、Dashboard、项目三视图（dnd-kit 看板/行内编辑表格/frappe-gantt 甘特）、BOM（齐套率环形图+导入向导字段映射）、Specs（Target vs Actual 判定+Mermaid）、Changes（ECO 时间轴+ECR 审批）、PLM 产品树/物料库、知识库（MD 编辑器+版本历史）、资源负载、admin 用户/审计
- seed 演示数据：4 角色账号（Demo@123456）、TH-100 项目（EVT 阶段，2 颗 Delayed 卡脖子物料联动 Block 组装任务）、GW-20、产品树、5 篇知识库文档

### 环境问题
- 本机 npm 网络极慢：官方源 40 分钟无产出；npmmirror 镜像可用但单包元数据 ~90s。安装命令需 `--fetch-retries=5`。
- 本地 editor_sdk 打开 docx 流式加载失败（document is not open），回退 Python 只读解析 word/document.xml 提取文档内容。

### 待办
- npm install 完成后：npm run setup（db push + seed）→ npm run build 验证 → 启动 dev 验证
